# v0.7.6 post-release verification

Status: **Pending publication**.

After GitHub creates the v0.7.6 prerelease, record the annotated tag object, tag/package commit, workflow run, exactly three published assets, downloaded byte sizes and SHA-256. Validate all downloaded assets from a fresh temporary directory and confirm that v0.7.5 remains unchanged.

This document records publication/package evidence only. Live BeamNG.drive 0.39.4 validation remains **Pending owner validation** until the repository owner executes `LIVE_TEST_PLAN.md` with the downloaded release ZIP.
