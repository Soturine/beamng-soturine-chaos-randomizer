# v0.7.6 live evidence template

Use one reviewed copy per `LIVE_TEST_PLAN.md` case.

## Artifact identity

```text
Case ID / result / tester / date / timezone:
Release URL:
ZIP filename / bytes / SHA-256:
Tag object / tag commit / manifest commit:
BeamNG full build:
```

## Environment

```text
OS / renderer / GPU / VRAM / CPU / RAM:
Locale / UI scale / safe zone / alignment:
AppHost measured width and height / compact state:
Map / traffic / world vehicle count:
Enabled mods and clean-profile status:
Input: mouse / keyboard / controller / UINav:
```

## Reproduction and evidence

```text
Seed / action / settings / locks / Event preset:
Steps and expected result:
Observed outcome / applied state / confidence / warnings:
Operation ID / generation / mode / phase code / deadline / semantic progress:
Source / candidate / accepted IDs and VehicleIdentity authority:
Race slot / player focus / staging and final transforms:
Expected remove-add sets / external world delta:
Preview requested/data/rendering/visible state / drawn markers / renderer error:
Reload / readback / repair counts and phase durations:
Persistence category / retry / scheduler terminal state:
AI mode / role / target / Stop AI result:
```

Attach the diagnostic export, relevant log interval and screenshots/video. Redact personal paths and secrets. Do not update aggregate counts until the evidence is reviewed against the exact published ZIP.
