# v0.7.4 live evidence template

Use one copy per case from `LIVE_TEST_PLAN.md` and test the downloaded published ZIP.

## Identity

```text
Case ID:
Result: Passed | Failed | Blocked | Not applicable
Tester/date/timezone:
Release URL:
ZIP filename / bytes / SHA-256:
Tag commit / manifest commit:
BeamNG full build:
```

## Environment

```text
OS:
Renderer:
GPU/vendor/VRAM:
CPU/RAM:
Locale/runtime locale ID:
UI scale/alignment/safe zones:
AppHost measured width/height:
Map/traffic/world vehicle count:
Enabled mods and clean-profile status:
```

## Reproduction and result

```text
Seed / action / settings / locks / Race policy:
Preview origin / heading / formation / spacing / margin:
Exact steps:
Expected terminal outcome:
Observed terminal outcome:
Operation/race/slot IDs and generations:
Source/candidate/accepted/owned vehicle IDs:
Expected remove/add sets and external world delta:
RuntimeIntegrity / Drivability / ChaosAcceptance / fluid state:
Phase/overall progress and watchdog age:
Reload/readback/repair counts and durations:
Player focus before/during/after:
P/A/B/C physical IDs and transforms:
Preview kind/slot states/actual-vs-fallback bounds:
Peak owned temporary count:
FPS / 1% low / CEF memory (when applicable):
```

Attach the diagnostic export, relevant `beamng.log` interval, screenshots/video, and before/after world vehicle list. Redact personal paths and secrets. Do not edit aggregate counts until the evidence is reviewed against the exact published asset.
