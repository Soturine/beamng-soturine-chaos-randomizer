# v0.7.3 live evidence template

## Identity

```text
Case ID:
Result: Passed | Failed | Blocked | Not applicable
Tester/date/timezone:
Release URL:
ZIP filename / bytes / SHA-256:
Tag commit / manifest commit:
BeamNG full build:
```

## Environment

```text
OS:
Renderer:
GPU/vendor/VRAM:
CPU/RAM:
Locale/runtime locale ID:
UI scale/alignment/safe zones:
Map/traffic/world vehicle count:
Enabled mods and clean-profile status:
```

## Reproduction and result

```text
Seed / mode / settings / locks:
Exact steps:
Expected terminal state:
Observed terminal state:
Operation/race/slot IDs and generations:
Source/candidate/accepted/owned vehicle IDs:
Phase/overall progress and watchdog age:
Peak owned temporary count:
FPS / 1% low / CEF memory (when applicable):
```

Attach the diagnostic export, relevant `beamng.log` window, screenshots/video,
and before/after world vehicle list. Redact personal paths and secrets. Do not
edit the global counts until this evidence is reviewed against the exact
published asset.
