# v0.7.7 live evidence template

Use one reviewed copy per case in `LIVE_TEST_PLAN.md`.

## Artifact and environment

```text
Case / result / tester / date / timezone:
Release URL / ZIP filename / bytes / SHA-256:
Tag object / tag commit / manifest commit:
BeamNG full build / renderer / OS / GPU / CPU / RAM:
Locale / UI scale / safe zone / AppHost width x height / compact state:
Map / traffic / world vehicle count / installed mods:
Player model/config / input method:
```

## Operation and lineage

```text
Action / preset / seed intent / episode seed:
Operation ID / generation / phase / outcome / applied state / confidence:
Slot ID / slot seed / generation-placement-drivable-AI readiness:
Candidate model/config/ID / accepted concrete ID / managed handle / placement ID:
Ownership and local authority evidence:
Old/new/removed owned IDs / temporary and peak counts / unrelated world delta:
Policy profile/rule/severity/evidence/decision:
AI behavior/target/command/dispatch/readback/failure:
Preview data/renderer capability/attempt/rendered markers/fallback/error:
Recovery action and result:
```

## Reproduction

```text
Steps and expected result:
Observed result:
Player/camera focus before/during/after:
Logs, diagnostic export, screenshots/video:
Reviewer decision and rationale:
```

Redact personal paths and secrets. Missing evidence is Pending, never Passed.

