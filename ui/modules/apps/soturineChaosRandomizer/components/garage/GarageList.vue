<template><div class="scr-dna-list" v-bng-scoped-nav="{ type: 'container' }"><VehicleDNACard v-for="entry in entries" :key="entry.id" :entry="entry" :busy="busy" :selected="selectedId === entry.id" @select="$emit('select', $event)" @restore="$emit('restore', $event)" @replay="$emit('replay', $event)" @favorite="$emit('favorite', $event)" /></div></template>
<script setup>import { vBngScopedNav } from "@/common/directives"; import VehicleDNACard from "./VehicleDNACard.vue"; defineProps({ entries: { type: Array, default: () => [] }, selectedId: String, busy: Boolean }); defineEmits(["select", "restore", "replay", "favorite"])</script>
