<template><article class="scr-dna-card" :class="{ 'is-selected': selected }" bng-nav-item><button type="button" class="scr-card-main" @click="$emit('select', entry)"><ThumbnailViewer :entry="entry" /><span><strong>{{ entry.name }}</strong><small><code>{{ entry.modelKey || entry.final?.modelKey || 'unknown' }}</code> · {{ entry.updatedAt || entry.createdAt || '—' }}</small></span></button><div class="scr-actions"><button type="button" :disabled="busy" @click="$emit('restore', entry)">{{ t('garage.restoreExact') }}</button><button type="button" :disabled="busy" @click="$emit('replay', entry)">{{ t('garage.replay') }}</button><button type="button" :aria-pressed="entry.favorite === true" :disabled="busy" @click="$emit('favorite', entry)">★</button></div></article></template>
<script setup>import { useStores } from "../../stores"; import ThumbnailViewer from "./ThumbnailViewer.vue"; defineProps({ entry: { type: Object, required: true }, selected: Boolean, busy: Boolean }); defineEmits(["select", "restore", "replay", "favorite"]); const { i18n: { t } } = useStores()</script>
