<template><SegmentedControl :model-value="modelValue" :label="t('nav.race')" :items="steps" @update:model-value="$emit('update:modelValue', $event)" /></template>
<script setup>import { useStores } from "../../stores/index.js"; import SegmentedControl from "../common/SegmentedControl.vue"; defineProps({ modelValue: { type: String, required: true } }); defineEmits(["update:modelValue"]); const { i18n: { t } } = useStores(); const steps = ["cars", "placement", "drive"].map(value => ({ value, label: t(`race.${value}`) }))</script>
