-- Historical import path retained for third-party integrations and saved tooling.
-- New production code uses the Race domain through raceManager.lua.
return require("ge/extensions/soturineChaosRandomizer/compat/legacyLineupFacade")
