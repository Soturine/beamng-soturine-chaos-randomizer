-- The persisted format and public method names still use "lineup" for backward
-- compatibility. Keep that vocabulary at this boundary only.
return require("ge/extensions/soturineChaosRandomizer/raceManager")
